# FitTracker — Current Status

Срез: 25.09.2026 (main) [VF-P1 — верификация 24.09 исправлена 9/10, SCR-1/SP-1]


Источник фактического состояния — текущий `main`. Если документ расходится с кодом, код имеет приоритет, после чего документ актуализируется.

## 1. Легенда

- ✅ готово
- 🟡 частично / есть остаток
- 🔲 открыто
- ⏸️ сознательно отложено

Приоритет: 🔴 критичный · 🟠 высокий · 🟡 средний · 🟢 низкий.

---

## 2. Technical baseline

| ID | Трек | Пр. | Статус | Комментарий |
|---|---|---:|---|---|
| SEC-1…SEC-10 | Security | — | 🟡 | Основные security-проблемы закрыты; открытые пункты — SEC-11…SEC-15 (§13) |
| ARCH-1…ARCH-9 | Architecture | — | ✅ | Основные архитектурные проблемы закрыты; ARCH-8 (injury warnings) полностью на normalized tables |
| ARCH-10 | Architecture | — | ✅ | Аудит и очистка workout data flow: удалены неиспользуемые таблицы `workout_sessions` и `session_sets`; `workout_logs` зафиксирован как единственный source of truth для подходов |
| PERF-1…PERF-7 | Performance | — | ✅ | Основные проблемы закрыты; Workout остаётся зоной profiling |
| SCALE-3…SCALE-7 | Scalability | — | ✅ | Основные пункты закрыты |
| SCALE-1 | Testing | 🟢 | 🔲 | Автотесты отложены до релиза; чистые функции готовы к покрытию |
| SCALE-2 | Monitoring | 🟢 | 🔲 | Sentry до production build (отложено) |
| RPC-1…RPC-3 | RPC | — | ✅ | Security/transaction RPC реализованы |
| DATA-1 | Data migration | — | ✅ | Reference data (equipment/injuries/alternatives) полностью на normalized tables; legacy columns dropped |
| PERF-8, PERF-10|Performance/Design | 🟠 |🔲|См. секции 12 и 13: baseline метрик, React Query audit. PERF-9 — ✅ (FlashList, §13)|
| DS-1 | Design system | 🟠 | ✅ | Аудит токенов/типографики/spacing/states завершён. Контраст textTertiary исправлен (WCAG 2.1 AA). Этап H4: accessibilityRole/Label/Hint во всех новых поверхностях (Фичи 1–7, UX-15): StrengthLevelBadge, WeeklyReviewSection, StatusCard (readiness pips, cycle/injury/forecast/pain chips), ExerciseSettingsSheet (степперы/интенсивность/save), workouts.tsx (sticky/segmented/items/skip sheet). PainTrendSheet/WorkoutForecastSheet — без интерактивных элементов, изменений не требуют. |
| DS-2 | Design system | 🟠 | ✅ | Bottom Tab Bar: внедрён паттерн Pill Highlight (PRODUCT.md §3.6). Активный таб имеет pill-shaped background (`colors.primary`), контрастный цвет иконки/текста (`textInverse`), увеличенный `strokeWidth` (2) и жирный текст (`600`). Заливка иконок убрана для сохранения calm interface и предотвращения визуальной перегрузки. |

## 3. Existing product baseline

| ID | Область | Статус | Что уже есть |
|---|---|---|---|
| FIT-1 | Programs | ✅ | activation/deactivation/status |
| FIT-2 | Programs | ✅ | sync правок в будущие workouts |
| FIT-3 | Programs | ✅ | robust delete + invalidation |
| FIT-4 | Dashboard | ✅ | выбор программы при отсутствии active program |
| FIT-5 | Workouts | ✅ | следующая фаза/неделя |
| FIT-6 | Workout | ✅ | program + phase context |
| FIT-7 | Workouts | ✅ |пропуск тренировки программы: long press на «Следующая» → подтверждение, skipped_at + advanceProgramProgress, бейдж «Пропущена», в историю не попадает|
| FIT-8 | History / Progress / Dashboard | ✅ | **effective date для тренировок** (bugfix 2026-08-23): исправлено использование `created_at` как даты тренировки в History/Progress/Dashboard/Streak/PR; effective date = `finished_at ?? started_at ?? created_at` (CLAUDE.md §4); хелпер `effectiveDate()` в historyService.ts, аналогичные inline-вычисления в dashboardService / progressService / weeklySummaryService |
| FEAT-1.1 | Tracker | ✅ | per-set previous data + progression chips; chips ручной корректировки доступны через «Изменить» в RecommendationCard (COACH-1) |
| FEAT-1.2 | Tracker | ✅ | rest timer + auto-start |
| FEAT-1.3 | Tracker | ✅ | streak |
| FEAT-1.4 | Progress | ✅ | e1RM / PR |
| FEAT-1.STD | Progress | ✅ | **Strength Standards** (Фича 1): бейдж силового уровня (Novice/Beginner/Intermediate/Advanced/Elite) в Progress Hub (StrengthTrendChart) и PR-карточках (PersonalRecordsCard). Тап → SheetShell с таблицей нормативов (e1RM/вес). Нормативы для 13 compound-движений, male/female; для упражнений без нормативов — бейдж не рендерится (честный fallback) |
| FEAT-1.5 | Tracker | ✅ | plate calculation logic + UI (PlateMathRow, настройки веса грифа в profile/settings) |
| P0.2 | Tracker | ✅ | Reps per side (unilateral exercises: reps_left/reps_right + UI + engine fallback) |
| P1.1 | Training Engine | ✅ | Progression policies (linear, double_progression, greyskull, time-based) + UI picker + explanation
| FEAT-1.6 | History | 🟡 | CSV builder есть, service/UI отложены |
| FEAT-1.7 | Tracker | ✅ | RPE 1–10, tappable scale — канон |
| FEAT-1.8 | Context | ✅ | optional daily readiness (P0: expanded with sleep/stress details, auto-calculation, and L3 trends) |
| FEAT-1.9 | Safety | ✅ | pain sheet + injury caution + persistent state (PR6): prefill из pain_events, upsert/delete, «Боль прошла», visual affordance «⚠ Боль отмечена» |
| FEAT-2.2 | Progress | ✅ | WeightTrendChart + MetricSparkline + body metrics |
| FEAT-2.3 | Progress | ✅ | Разделение бедра на левое/правое (thigh_left_cm/thigh_right_cm) для консистентности с другими конечностями; thigh_cm — legacy (как arm_cm); миграция 20260823_split_thigh_cm.sql копирует данные в обе новые колонки; UI автоматически поддерживает через METRIC_FIELDS |
| UX-1 | Workout | ✅ | header разгружен, UnitToggle рядом с timer |
| UX-14 | Workout | ✅ | header variant D: Settings справа от названия, metadata слева, actions-bubbles справа |

## 4. Current UX work — Tracker

> Срез: Ветка `feature/workout-ux-rework` смержена в main; все ID закрыты.

| ID | Пр. | Статус | Цель |
| --- | --- | --- | --- |
| UX-2 | 🔴 | ✅ | пполный аудит workout/[id].tsx и progressive disclosure — выполнено: display modes + секционная структура + AlternativeExerciseCard (PR5) + pain persistent state (PR6) + knowledge subheadings (PR7) + split workout/[id].tsx (PR8) |
| UX-3 | 🔴 | ✅ | context sheets: history / technique / warm-up / pain / notes —  закрыто : technique через accordion с подзаголовками (PR7), pain persistent state с prefill и delete (PR6), warm-up через вкладку WorkoutTabs + WarmupBlock, history — per-set previous data + вкладка History с деталями тренировок; notes отложены (нет таблицы, не подтверждена потребность) |
| UX-4 | 🔴 | ✅ | доступные alternatives без перегрузки — выполнено: slider + text affordance + облегчённая AlternativeExerciseCard (PR5): Польза/Риски/Противопоказания видимы сразу, Техника в lazy-mount аккордеоне |
| UX-5 | 🔴 | ✅ | temporary vs program replacement + skip workout — закрыто: Alert 3 кнопок (Отмена/Только сегодня/В программе destructive) + rollback для seeded программ + graceful degradation; пропуск тренировки программы: long press на «Следующая», skipped_at + advanceProgramProgress, бейдж «Пропущена» (FIT-7) |
| UX-6 | 🟠 | ✅ | динамическая расшифровка RPE + быстрый skip — выполнено: «RPE N — description» в SetFeedbackEditor (FEAT-7 v2), кнопка «Пропустить» в редакторе (UX-6) |
| UX-7 | 🟠 | ✅ | настройка частоты запроса RPE — выполнено: useRpeSettings (always/last-set/off), picker в settings, conditional rendering чипа в SetsGrid (UX-7) |
| UX-8 | 🟠 | ✅ | lazy mount тяжёлого контента — media/slider монтируется только при раскрытии accordion; stagger в ExerciseSlider |
| UX-9 | 🟠 | ✅ | History calendar с отметками — выполнено: HistoryCalendar (месяц, навигация, точки, тап по дню → DaySummaryCard через SheetShell); даты вычисляются локально из уже загруженных данных (ноль новых запросов); пропущенные тренировки не отображаются (Вариант A, консистентно с FIT-7) |
| UX-10 | 🟠 | ✅ | Calendar/List toggle + day details — выполнено: HistoryViewToggle segmented control + useHistoryView (persist в AsyncStorage, default calendar); list view сохранён без изменений; day details: тап по дню → DaySummaryCard → history/[id] |
| UX-11 | 🟠 | ✅ | **Progress hub** (отдельный bottom-tab, единый экран «Как я меняюсь?»): убраны 4 режима; последовательный поток: Hero → Stats → Insights → Activity → Strength (с интерактивным селектором упражнений и explainability e1RM) → Weight → PR (в виде карточек-достижений) → RecentWorkouts (детерминированный градиент, program_name, duration, avg_rpe); History вынесен в отдельную ментальную модель (PRODUCT.md §11). **Реструктуризация (08.09.2026)**: контент сгруппирован в 4 чёткие визуальные зоны (Обзор, Инсайты, Динамика и рекорды, История) с общим заголовком раздела для устранения ощущения «хаоса» и водопада карточек. В Workout Report (`app/progress/[id].tsx`) заголовок навигации теперь отображает реальное имя тренировки (`data.name`) вместо generic «Отчёт». **Inline Accordion (09.09.2026)**: `WeeklyReviewSection` переведён с Bottom Sheet на Inline Accordion (L2 progressive disclosure). Устранена проблема неудобного скролла к нижнему листу; детали раскрываются плавно внутри карточки с haptic feedback. |
| UX-12 | 🔴 | ✅ | display modes для workout cards (training/balanced/learn) + picker в settings |
| UX-13 | 🟠 | ✅ | секционная структура ExerciseCard + вынос Equipment из accordion + подзаголовки через SectionSubheading в «Техника выполнения» и «Важно знать» (единообразие, PR7) + финальная UX-корректировка: SetsGrid перемещён выше Technique/Knowledge (главный рабочий блок карточки) |
| UX-15 | 🟠 | ✅ | Рефакторинг экрана «Тренировки»: убрана дублирующая sticky-карточка «Следующая»; прогресс программы компактен в ListHeader; первая карточка списка визуально выделена (плотный фон) и является якорем; PillToggle заменён на SheetShell-фильтр (Предстоящие / Эта неделя / Все). Дефолт: Предстоящие. |
| UX-16 | 🔴 | ✅ | **Уплотнение карточки упражнения**: старт/финиш в header (pill вместо footer), exclusive tags equipment/muscles, Info-tabs вместо двух аккордеонов, RPE onboarding-overlay над таблицей, recommendation под 💡 в header сетов, rest pill + Info в один ряд. **Визуальные фиксы (14.09.2026)**: gap между карточками (SPACING.md), per-cell green (только введённые значения), RPE overlay на всю ширину с анимацией из тапнутой колонки, pill «Таймер» (без пресета), secondary muscles текст читаемый (textSecondary + цветная точка), overflow protection (maxWidth+ellipsis+wrap) |
| WARMUP-1 | 🟠 | ✅ | **Переделка блока разминки (24.09.2026)**: карточка разминки была ~3 экрана высотой, техника с картинками скрыта в аккордеоне под слайдером альтернатив (пользователь не видел картинки вообще), тап по альтернативе сразу заменял (изучить было нельзя), «аналоги» = пересечение мышц без relation_type. Теперь: (1) L1 `WarmupExerciseCard` компактный — номер-чекбокс (тап = выполнено), миниатюра техники из `media_url` видна сразу, длительность/тип/таймер; мышцы/оборудование убраны (дублировали заголовок блока); (2) L2 `WarmupExerciseSheet` (SheetShell) — `TechniqueMediaSlider` наверху без аккордеона, описание техники, Польза/Риски/Противопоказания аккордеонами, секция «Похожие варианты»: просмотр аналога внутри листа (назад/CTA «Заменить»), бейджи relation_type (Прогрессия/Упрощение/Вариант, семантика ENG-5); (3) `getWarmupAlternatives` — сначала именованные аналоги из `exercise_relationships` (approved/suggested, в БД 235 исходящих связей из пула, 61 внутрь пула), добор по пересечению мышц; (4) альтернативы грузятся лениво по открытию листа, а не 20 строк × 7 карточек на маунте вкладки. **Фиксы (тот же день)**: (1) лист был bare SheetShell внутри ScrollView — панель обрезалась клипом скролла; state и рендер подняты в корень `workout/[id].tsx` (structura как у confirm-листа «Финиш»); (2) **Системный дефект SheetShell, приехавший в a42abc8 (DA-P1 «переработка SheetShell»)**: в `panelStyle` использовался `entering = interpolate(enter, [0,1],[1,0])` там, где нужен сам `enter` → при fully-open (enter=1) панель получала opacity 0 и translateY = экран вниз, а бэкдроп — полную темноту. Симптом на всех bottom sheet: «мелькнул и закрылся» (16 мс withDelay до старта анимации) → «только затемнение». Лечится одной строкой: `opacity: enter.value`, `translateY: dragY + (1 - enter.value) * H` (проверено численно + gates).guard'ы от «протекающего тапа» (`closableSV`, 600 мс) оставлены как защитная мера (не являются лечением корня; при желании снимаются одним откатом блока в SheetShell). Позитивный побочный эффект итераций: state и рендер warmup-листа подняты в корень `workout/[id].tsx` — вне ScrollView absolute-оверлей и должен жить. **Аудит всех SheetShell/Modal-поверхностей (25.09)**: 13 plain-листов с переключаемым `visible` — guard работает; `ProgramFormSheet` — условный маунт, guard на маунте; Modal-листы — на Android Modal размонтирует детей при скрытии (remount → guard обновляется), на iOS hit-test фиксирует тач на старом респондере (протечки нет) → центрального guard достаточно; найдены и исправлены дефекты рядом: `ExerciseSettingsModal` (самодельный бэкдроп вне SheetShell) получил такой же 600-мс guard, `DaySummaryCard` и `NutritionLogListModal` — обязательный `isModal` внутри нативного Modal (грабля Fabric, §12). Gates: tsc ✅, eslint ✅ |

| WT-2 | 🟠 | ✅ | Единый pill-таймер вместо дубля «Начать»+панель (правками 25.09): кнопка «Начать» убрана из шапки; pill = idle «Начать» (тап — старт), running — время+chevron (тап — панель с паузой), paused — обводка warning + тап = продолжить. Исправлен двойной вызов handleTimerStart (двойная запись started_at; теперь идемпотентно через startedSavedRef, восстановление сессии не перезаписывает). Убран дублирующий Alert подтверждения в saveWorkout (остался только confirm-лист шапки). «Завершить» видно при isWorkoutActive || saving |
| SG-2 | 🔴 | ✅ | Два дефекта сетов: (1) пустая тонированная рамка под «Подходы» (контейнер подсказки рисовался без видимых детей при свёрнутой 💡/отсутствии истории) — гейт hintVisible по наличию хотя бы одного ребёнка; предикат PlateMathRow вынесен в `plateMathVisible` (общий для компонента и гейта). (2) ячейки веса/повторов иногда становились некликабельными и теряли значение (залипший focusedRef при детаче вью + коммит только в onBlur) — SetInput переведён на debounce-коммит 350 мс + коммит в blur и при unmount; removeClippedSubviews убран из FlatList тренировок и горизонтального ScrollView ExerciseSlider (не возвращать — роняет responder/blur TextInput'ов) |
| WU-2 | 🟠 | ✅ | Запоминание замен разминки между тренировками: таблица `warmup_preferences` (миграция 20260925103000, применена к проду: новая пустая таблица, 4 RLS-политики по auth.uid(), upsert-уникальность (user_id, origin_exercise_id); откат = DROP TABLE). При «Заменить…» в листе — upsert предпочтения (ошибка сети не откатывает локальную замену); generateWarmup шаг 7 подставляет запомненные варианты с валидацией (в пуле разминки + не противопоказан активным травмам, 2 прохода с visited-guard для цепочек A→B→C); сброс — долгий тап на ⟳ в шапке блока (Alert). database.types.ts: запись добавлена вручную по формату генератора (CLI-линковки нет) — сверить при ближайшей полной регенерации |
| DASH-PR | 🟡 | ✅ | Блок «4 личных рекорда» на Dashboard (ContextInsightCard) вёл на Unmatched Route: его onPress толкал удалённый `/profile/progress` (Progress hub переехал в bottom-tab ещё в UX-11, ссылка не обновилась). Исправлено + найдены и починены ещё два мёртвых href той же природы: StatusCard `/(tabs)/profile/injuries` → `/profile/injuries`; finish-ветка useWorkoutSession `/(tabs)/history` → `/(tabs)/progress`. Дополнительно: инсайт «N личных рекордов» в «Коротко о неделе» стал тапабельным (→ прогресс-хаб) и показал subtitle, который engine отдавал, но UI терял. Примечание: `PersonalRecordsCard.tsx` не импортируется нигде (dead code) |
| SP-1 | 🟡 | ✅ | Ритм между карточками упражнений на workout-экране: высота горизонтального слайдера растягивалась самой высокой страницей (карточкой-альтернативой) — после таких блоков зазор был больше. Теперь высота слайдера = высота основной карточки (onLayout → height), альтернативы при нехватке места скроллятся вертикально внутри своей страницы; подпись ENG-5 «N вариантов скрыто» перенесена из-под слайдера над ним — низ блока всегда низ карточки, зазор = SPACING.md у всех |
| SCR-1 | 🟠 | ✅ | Фризы вертикального скролла тренировки («замирает на секунду»): страницы альтернатив монтировались автоматически (InteractionManager после fetch) — до N полных AlternativeExerciseCard на каждое упражнение в окне, монтаж попадал в середину скролла и блокировал JS. Теперь altsMounted ставится только первым горизонтальным свайпом слайдера; fetch/ранжирование без монтажа страниц дёшевы. Если фризы останутся — снимать [FREEZE]-логи из Metro (useFreezeDetector, порог 100мс) и смотреть maxToRenderPerBatch FlatList как следующего кандидата |

### Остаточные аудиты экранов (из архивного `UX_AUDIT_PLAN.md`, Этап H)

Экраны, не покрываемые другими секциями §4–§9. Ветка `feature/workout-ux-rework` закрыта полностью (UX-2…UX-13); ниже — остаток по архивному UX_AUDIT_PLAN.

| ID | Пр. | Статус | Цель |
|---|---:|---|---|
| AUDIT-1 | 🟠 | ✅ | **Dashboard упрощён и дополнен**: фокус на Today-first; L1-summary питания — карточка с 2 страницами (swipe + тапабельные точки): стр. 1 — 3 концентрических кольца (внешнее — макросы сегментами пропорционально целям, среднее — калории, внутреннее — вода только при >0) + 🔥 сожжённые калории в центре + модалка быстрого ввода (state в index.tsx, рендер вне ScrollView); стр. 2 — lazy-таблица КБЖУ+вода за неделю; сожжённые калории перенесены с профиля на Dashboard (getBurnedCalories достаёт вес из profiles сам, null при отсутствии веса — бейдж скрыт); блок «Коротко о неделе» (insights); readiness остаётся optional (PRODUCT.md §12) |
| AUDIT-2 | 🟡 | ✅ | аудит проведён, вердикт 🟢, кодовые правки не требуются: alternatives работают через exerciseReferenceService (normalized tables, INVENTORY §10); техника — текст, основной контент экрана; records staleTime 2 min — практического импакта нет. Не подтверждено (предположение): hero media autoPlay в TechniqueMediaSlider |
| AUDIT-5 | 🟠 | ✅ | **Workout Report**: создан `app/(tabs)/progress/[id].tsx` с детальной сводкой (время, объём, подходы, ср. RPE), задействованными мышцами и списком упражнений с логами |
| AUDIT-3 | 🟡 | ✅ | аудит библиотеки упражнений: sort sheet → SheetShell ✅ (INVENTORY §6); поиск/фильтры/infinite scroll/PERF-9 ✅; autofocus на поиске убран ✅; empty state улучшен (дружелюбный текст + четкий CTA «Сбросить фильтры») ✅ |
| AUDIT-4 | 🟡 | ✅ | аудит проведён (PRODUCT.md §15): Goals/Injuries/Metrics — 🟢; Profile — 🟢. Modal→SheetShell в profile/metrics/settings — ✅. Закрыто ✅: pharma tooltip (GoalsStep2), «Пересчитать» с иконкой RefreshCw (GoalsStep3), body zones-фильтр в injuries (arms/torso/legs с haptics), pagination истории в metrics (HISTORY_PAGE=30 + «Показать ещё»), кликабельные карточки статистики и PR в profile.tsx (router.push + ChevronRight), **chart chips tooltip в metrics** ✅ (long-press показывает inline-карточку с трендом, значением и датой). |
| AUDIT-6 | 🟠 | ✅ | **Блок «Состояние сегодня»** (`StatusCard`): readiness — мини-кольцо (consistency с `CircularNutritionChart`) + 5 tappable pips (quick-set с haptics, `ReadinessSheet` — детальный check-in); чипы активных травм (`SEVERITY_COLORS`, tap → `/profile/injuries`) + информационный чип «⚠ Боль сегодня» (`painService.getPainEventsToday`); строка-следствие с приоритетом safety > recommendation (PRODUCT.md §8); `ReadinessSheet` встроен в `StatusCard` и инвалидирует `['todayReadiness', userId]` после сохранения; травмы и readiness — независимые сигналы, травмы не переписывают самооценку |
| AUDIT-6 | 🟠 | ✅ | **Блок «Состояние сегодня»** (`StatusCard`): readiness — мини-кольцо (consistency с `CircularNutritionChart`) + 5 tappable pips (quick-set с haptics, `ReadinessSheet` — детальный check-in); чипы активных травм (`SEVERITY_COLORS`, tap → `/profile/injuries`) + информационный чип «⚠ Боль сегодня» (`painService.getPainEventsToday`); строка-следствие с приоритетом safety > recommendation (PRODUCT.md §8); `ReadinessSheet` встроен в `StatusCard` и инвалидирует `['todayReadiness', userId]` после сохранения; травмы и readiness — независимые сигналы, травмы не переписывают самооценку |
| NUTRI-1|🔴|✅|сожжённые калории на Dashboard: исправлен фильтр по finished_at (завершённые тренировки) вместо created_at; тренировка, завершённая вчера, теперь корректно учитывается|
| NUTRI-2|🟠|✅|CRUD записей питания: список за день в L2-модалке (NutritionLogListModal), редактирование через prefill NutritionAddModal, удаление с подтверждением; инвалидация daily/weekly/nutritionLogs кэшей|

### Дизайн-аудит 23.09.2026 (полная поверка всех поверхностей по PRODUCT.md §3.1–3.6)

Вердикт: тренировочный флоу, списки и семантика состояний — сильные стороны; системные дефекты сфокусированы в нескольких повторяющихся местах (единицы кг/lb только в workout-флоу, SheetShell без анимации и с фиксированной полувысотой, тап-зоны <44pt кластером, 3 паттерна loading, error рендерится как empty на 2 экранах).

| ID | Пр. | Статус | Цель |
|---|---:|---|---|
| DA-P0 | 🔴 | ✅ | Три функциональных дефекта: (1) чек-ин цикла с Dashboard не сохранялся — заглушки в `StatusCard` заменены на реальные вызовы `cycleService.upsertCycleEvent/deleteCycleEvent` + инвалидация `['cycleEvents', userId]` (паттерн profile.tsx); (2) `useProfile` глотал ошибку (вечная «Загрузка...») — добавлен флаг `error`, на профиле error-ветка с «Повторить»; (3) мёртвый тернарник единиц в `ExerciseProgressCard` (`'кг' : 'кг'`) — значение и единица теперь учитывают `useUnitPreferences` (kg/lb). Gates: tsc ✅, eslint ✅ |
| DA-P1 | 🟠 | ✅ | (A) единицы кг/lb в аналитике — ✅ (hook `useWeightDisplay`, ~20 мест: metrics/progress/profile/records/day-summary/weekly-review/strength-badge/insights/rpe/forecast + engine-строки progression/weeklySummary/muscleLoad через параметр `unit`, React Query кэш-ключ с unit); (B) SheetShell — ✅ (slide-up 240мс, высота по контенту ≤85%, grabber, swipe-dismiss, API-совместимость с 21 потребителем); (C) тап-зоны — ✅ (pill старт/финиш и чипы StatusCard/ProgramCard/фильтров через hitSlop без визуальных изменений; search/sort-кнопки 40→44, табы minHeight 44); (D) loading — ✅ (ListSkeleton вместо spinner/текста/пустого списка: dashboard, progress, profile, programs, workout/[id]); (E) error-state — ✅ (progress.tsx: error-ветка useHistory/useProgress с «Повторить»; programs.tsx: `error`/`retry` из usePrograms, renderError вместо empty). Gates: tsc ✅, eslint ✅ |
| DA-P2 | 🟡 | ✅ | Консистентность: (1) dead code удалён (SetFeedbackEditor, WorkoutScreenFooter, progressBar*-стили); (2) SheetShell — единый паттерн монтирования (лист сортировки вынесен из ListHeader в корень programs.tsx); (3) withAlpha вместо конкатенации hex-альфы (~198 мест / ~78 файлов); (4) сегмент-контролы унифицированы на PillToggle (programs.tsx табы, вкладки ExerciseCardInfo, табы+периоды MuscleStatsSection; мёртвые tab*-стили из card/filter.ts удалены); осознанные исключения: анимированные WorkoutTabs/UnitToggle/MuscleLoadModeToggle, чипы с семантическими цветами per-option (ProgramFormSheet/ExerciseSettingsSheet/InjuryFormSheet/PainSheet/ReadinessSheet), 5-чип-row в NutritionAddModal; (5) мульти-акцент убран в строках статистики dashboard (календарь) и profile (плитки); (6) a11y-роли/метки в profile, dashboard-календаре, AppCard, ProgramCard; (7) CustomTabBar — скользящий pill (Reanimated withTiming 250мс; на устройстве не проверялся); (8) split 5 экранов >500 строк: settings 909→50 (6 компонентов в src/components/settings/), workouts 726→~320 (5 компонентов + src/utils/workoutsList.ts), metrics 670→118 (5 компонентов в src/components/profile/metrics/), dashboard 666→~330 (TrainingCalendarCard, WeeklyInsightsSection), exercises 649→~317 (ExerciseRow, MuscleGroupFilters, ExerciseSearchBar, ExerciseSortSheet, ActivationFilterChip). Gates: tsc ✅, eslint ✅ (проект целиком) |

### Аудит данных и формул 23.09.2026 (FD — корректность запросов/агрегаций/расчётов)

Вердикт: системные дефекты в слоях чтения аналитики — неверные имена колонок, потеря unilateral-повторов, смешение skipped/выполненных, 1000-row cap PostgREST, UTC-vs-local даты. Чинены по приоритету P0 → P1.

| ID | Пр. | Статус | Цель |
|---|---:|---|---|
| FD-P0 | 🔴 | ✅ | (FD-1) прогрессия была мертва — контекст сессии (фаза/deload/восстановление/цикл) не пробрасывался из экрана в движок: добавлен `ProgressionContext` и его передача в workout-флоу; (FD-2) `estimatedReps` не сохранялся в `workout_logs` — исправлено сохранение; (FD-3) `forecastService` читал несуществующую колонку `weight` → `weight_kg`; (FD-5) UTC-vs-local даты — единый локальный `toDateKey` во всех агрегациях. Gates: tsc ✅, eslint ✅ |
| FD-P1 | 🟠 | ✅ | (FD-6) 1000-row cap — клиентская пагинация `fetchAllPages` (детерминированный `.order('id').range()`) в history/progress/profile/muscleStats/weeklySummary(pre-week PR)/getPersonalRecords; (FD-7) единый фильтр «реальной тренировки» `.not('finished_at','is',null).is('skipped_at',null)` и `skipped_at==null` во всех агрегациях (dashboard/workoutsService/profile/muscleStats/history); (FD-8) unilateral-повторы — центральный хелпер `effectiveReps` (`reps ?? reps_left+reps_right`), все аналитические select несут `reps_left/reps_right`, volume/e1RM/PR/RPE-сайты пересчитаны; (FD-9) макросы — действующие safety floors (белок ≤2.4 г/кг и ≤35% ккал, жиры ≥1.0 г/кг и ≥20% ккал), убран ложный текст «модификатор фарма» в GoalsStep3 (banner: «не влияет на расчёт КБЖУ»); (FD-10) `createMetric` больше не перезаписывает `current_weight_kg` без учёта даты. **Caveat**: `effectiveReps` (L+R) применён и к e1RM/PR ради согласованности аналитики — e1RM для unilateral слегка завышен (двойной тоннаж), осознанный компромисс. Gates: tsc ✅, eslint ✅ |
| FD-1b | 🟡 | ✅ | Ревизия нюансов `progression.ts`: парсер «8+» (min=max=8 → increase при 8 повторах — корректно для «≥8»); разделение `factors.lastRpe` (хинт «прошлый раз») и `evalRpe` (решение по текущим сетам) — намеренный дизайн, живой recompute отнесён в COACH-1; порог `weeksInBlock > 6` — tuning-политика, не формульная ошибка. Correctness-багов не найдено, правок нет |
| FD-4 | 🔴 | ✅ | `progression_policy` проходит весь путь end-to-end (реализовано 25.09, вариант A, прод-write по явному подтверждению). Ре-аудит цепочки показал не 2, а 5 leak'ов, все закрыты: (1) прод-миграция `20260925161000` — колонка `workout_exercises.progression_policy text DEFAULT 'linear'` (2126 строк = 'linear' = прежнее фактическое поведение движка, 0 program_exercises имели нелинейную политику — проверено до применения) + `save_program_snapshot` теперь upsert'ит политику (INSERT/UPDATE с COALESCE — клиент без ключа не затирает сохранённое); тело функции взято побайтово из прод-дампа (репо не содержало — drift SEC-15 исправлен здесь же), baseline оригинала в `supabase/baseline/` (md5 старого тела сверен = `de6b8ba3…`), после применения md5 прод-тела = хеш файла (`86e8e547…`), SECURITY DEFINER/search_path/гранты сохранены, anon по-прежнему без execute (SEC-11 цел); откат = drop column + baseline-файл. (2) `useProgramEditor` snapshot добавлял `progression_policy` в payload — без этого picker в редакторе не доезжал до RPC. (3) `startNextWorkout`: select + insert копируют политику в снапшот тренировки. (4) `fetchWorkoutSession` embed select + `SessionWERow` + маппер: `ExerciseData.progression_policy` заполняется (раньше был всегда undefined → движок жил только 'linear', хотя ExerciseCard:226 читал поле). (5) `repeatWorkout` переносит политику в повтор. `database.types.ts` — колонка добавлена вручную в формате генератора (CLI-линковки нет, паттерн WU-2). Gates: tsc ✅, eslint ✅. На устройстве не проверялось — сценарий: выставить double_progression в редакторе → старт тренировки → чип политики в RecommendationCard + ветка движка |
| FD-P2a | 🟡 | ✅ | Ре-аудит 25.09 (детальные находки 23.09 не хранились, выведены заново: 2 параллельных проверки + сверка вручную). Исправлено: (FD11-1) chronic-окно ACWR исключает acute-неделю (`startISO` вместо `nextStartISO`) — новичок больше не видит ложный «ACWR 4.00, риск травмы» месяц; (FD11-2) `acwr<0.8` и fallback-`volumeRatio<=0.8` — reasons-only, не elevatedSignals (разгрузка больше не сама провоцирует highLoad); (FD11-3) `volumeByType` в сервисе — нули до миграции workout_type (ENG-10): инсайт TYPE_IMBALANCE ложно срабатывал ВСЕМ (strength=totalVolume → ratio 1), engine-гейты (<0.8) теперь сами глушат его и пороги P1.2; (FD11-5) cycle: при залогированном только начале месячных конец оценивается start+4дн (isEstimated) — раньше фаза «менструация» не заканчивалась никогда, овуляция/лютеиновая и их progression-правила были мертвы, календарь красный весь месяц (общий `resolveMenstruationEnd` в calculateCyclePhases и getPhaseForDate); (FD11-7) warmup-предпочтения: замена пропускается, если аналог уже в списке (дубликаты устранены); (FD11-9) сон/стресс/лютеиновая/овуляция вынесены из начала дерева в обёртку `calculateProgression`: downgrade **только increase→hold**, decrease (MAX_EFFORT RPE 10, OVERREACHED, SESSION_FATIGUE, PLATEAU_DELOAD) больше не затирается «держим вес»; коды причин и текст «Почему?» сохранены; (FD12-1) plate math: `plateWeight` конвертируется через `toDisplay` — lb-пользователь получал сборку от кг-числа; (FD12-2) `formatPlates` печатает `count×2` вместо всегда «2×»; (FD12-4) rest_seconds персистится вместе с target_sets (`updateWorkoutExerciseTargetSets` → `updateWorkoutExerciseSettings(patch)`); (FD12-5) `getBurnedCalories(days=1)` — границы дня локальные, не `+00:00` (вечерние тренировки не выпадают из «🔥 сегодня»). Gates: tsc ✅, eslint ✅. Не проверялось на устройстве |
| FD-P2b | 🟡 | ✅ | Верифицируемая часть P2b (без продуктовых решений), 25.09: (FD11-6) ✅ `cycle.ts normalizeDate` — календарная строка 'YYYY-MM-DD' парсится по локальным компонентам (раньше `new Date(str)` = UTC → в отрицательных TZ событие сдвигалось на день, границы фаз съезжали); (FD11-10) ✅ подтверждена механикой: PR требует pre-week baseline → у новичка `prs` пуст всегда и deload-сигнал «RPE растёт без рекордов» был систематически ложноположительным — guard `chronicVolume > 0` (4 недели предыстории); (FD12-6) ✅ проверено прод-данными (read-only SQL): `user_injuries` пуста → вред латентный, но в `injury_exercise_warnings` 62/279 строк с типом `other`/NULL, а предикат уровня 1 матчил тип сквозь зоны → ложные исключения; вынесен общий канон `contraindicationMatchesInjury` (constants/injuries.ts): зона совпала → исключение; по типу — только нетривиальному (не `other`/NULL); три копии (alternatives + 2 в warmupService) объединены, deprecated-легаси `matchesContraindication` не тронут. Gates: tsc ✅, eslint ✅ |
| FD-P2c | 🟡 | ✅ | Решения пользователя 25.09 реализованы: (FD12-3) ✅ «Вернуть оригинал» стал честным — снимок оригинала хранится в памяти экрана (`originalsRef`, первая замена в цепочке A→B→C запоминает A), возврат пишет обратно `workout_exercises.exercise_id` + болевые события (зеркально ENG-16) и восстанавливает карточку; при отказе записи — ошибка, бейдж остаётся; после перезахода экрана бейджа/кнопки нет (в памяти) — осознанный компромисс варианта «без прод-миграции», колонка `original_exercise_id` не потребовалась; (FD11-4) ✅ канон −30% веса: L2-лист «Разгрузочная неделя (Объём)» рендерит `deload.plan` (compound, текущий→новый вес/повторы, через `kgToUnit`), UI-текст «−40–60%» приведён к плану, −10% движка остался как отдельный механизм дилоад-фазы; (FD11-8) ✅ см. ENG-12 — мёртвая `generateWarmupSets` удалена, отметка снята. Остаток FD-P2c перенесён ниже. Gates: tsc ✅, eslint ✅ |
| FD-P2d | 🟡 | 🔲 | Остаток без решений: (FD12-7) lb-флоу в целях — ввод «Текущий вес (кг)»/рост в onboarding всегда кг/см, переключатель единиц в PreferencesSection локальный useState без персистентности; (FD12-8) floors макрос при низких ккал/высокой массе дают сумму ≠ целевым калориям (углеводы молча обнуляются) — нужен продуктовый вердикт «что показывать» |

### Аудит контента готовых программ 25.09.2026 (FD-SEED)

Вердикт: каталог seeded-программ страдал не от тренерской логики, а от дрейфа денормализованных данных. Проверено read-only SQL по проду + чтением движка; исправлено `supabase/migrations/20260925210500_fix_seeded_program_data.sql` (прод-DML применён 25.09 по явному указанию пользователя; пост-состояние просчитано симулятором до записи).

| ID | Пр. | Статус | Цель |
|---|---:|---|---|
| FD-SEED-1 | 🟠 | ✅ | Ремонт данных готовых программ и их копий (применено на прод 25.09, всего затронуто 937 строк `program_exercises`, ни одна не добавлена/удалена): (1) 18 seeded + 16 copy-строк с `exercise_id IS NULL` — именовались существующими упражнениями, но id был NULL, поэтому `injury_exercise_warnings`, подбор альтернатив и warmup-маппинг для них не работали (safety-дыра), а созданная тренировка наследовала NULL; заполнены по уникальному совпадению имени; (2) 18 строк «Махи гантели в стороны» указывали на «Махи гантели перед собой» — другое движение, карта мышц/замены/предупреждения считали переднюю дельту вместо боковой, retarget на «Махи гантели в стороны стоя»; (3) 79 seeded + 106 copy строк с `exercise_name` ≠ каноническому `exercises.name` («Румынская тяга», «Становая тяга», «Жим ногами в тренажере» с «е», «…«Молот»») — `DayCard.tsx` рендерит денормализованное поле, пользователи видели имя, отсутствующее в каталоге; нормализовано от каталога (авторитарный источник); (4) PPLUL press:pull 1.53/1.45/1.50 против 1.00–1.25 у остальных — приведён к 1.24/1.21/1.29 правкой только low/medium-аксессуаров (жим −1 подход, тяга +1), базовые тяжёлые движения и пользовательские объёмы не тронуты. Post-verify: `null_ids=0`, `name_drift=0`, передних махов 0. Откат — реверс-UPDATE по именам, зафиксирован в шапке миграции |
| FD-SEED-2 | 🟡 | 🔲 | Остатки, требующие продуктовых/движковых решений, данных не трогали: (a) `program_exercises.reps_range` хранит тайминг («30 сек», «40 сек» у планки) — у приложения нет концепции timed-упражнений, `parseRepsRange` даёт `{30,30}`, decrease защищён (`Math.max(0,…)`/null для bodyweight), а increase будет «поднимать вес» после 30 секунд; нужен вердикт (поле `unit`/тип упражнения vs ограничение ввода); (b) StrongLifts 5×5: press:pull 2.00 и задняя цепь 1 подход/нед — это канон программы, менять нельзя без потери идентичности, но в описании стоит честно указать отсутствие вертикальной тяги/сгибаний; (c) во всех multi-phase программах объём силовой фазы ≥ гипертрофийной (58→64, 86→93, 98→111) при описаниях «гипертрофия → сила» — вопрос канона периодизации (объём↓ при интенсивности↑ vs равный объём), не баг; (d) 6 существующих seeded-программ живут только в проде, seed-миграций в репозитории нет → `supabase start`-replay их не воспроизводит (тот же класс, что SEC-17/18) |

### RPE-1: цепочка целевого RPE (25.09.2026, прод-DDL/DML по явному указанию пользователя)

Вердикт: фича ENG-17 (авторегуляция по целевому RPE) была задокументирована как готовая, но end-to-end не работала — значение нельзя было сохранить. Движок и UI исправны, все дыры оказались в серверных write-путях.

| ID | Пр. | Статус | Цель |
|---|---:|---|---|
| RPE-1 | 🔴 | ✅ | Три независимых leak'а закрыты миграциями `20260925223000_fix_target_rpe_persistence.sql` и `20260925224000_carry_rpe_into_scheduled_workouts.sql` (применены на прод): (1) `save_program_snapshot` не имел `target_rpe` ни в INSERT, ни в UPDATE — редактор принимал шкалу и молча её выбрасывал (в проде 0 из 937 строк `program_exercises` и 0 в `workout_exercises` с `target_rpe`); теперь INSERT пишет `(nullif(payload->>'target_rpe',''))::smallint`, UPDATE — `CASE WHEN jsonb_exists(payload,'target_rpe') THEN … ELSE target_rpe END`: явный null (сброс в «—») очищает поле, а сборка без ключа не затирает сохранённое (умолчание по наличию ключа, а не COALESCE, как в FD-4 для политики); (2) `copy_program_for_user` копировал 8 колонок из 10 → при копировании готовой программы терялись `target_rpe` **и** `progression_policy` (обе ветки: phased и fallback); (3) `create_workouts_for_program` вставлял `workout_exercises` тем же списком из 8 колонок → запланированные из программы тренировки не получали ни RPE, ни политику (FD-4 закрыл только клиентский `startNextWorkout`). Клиент менять не потребовалось: `ExerciseSettingsSheet.tsx:354` и `useProgramEditor.ts:222` уже отправляли `target_rpe`. Техника: тела взяты побайтово из прод-дампов, изменены только блоки с меткой `[RPE-1]`; генератор проверял обратимость (md5 тела без блоков == прод-оригиналы `86e8e547…`, `e1fa33bf…`, `c22b78d6…`), после применения — `dd169370…` (7861), `f4cff48e…` (3557), `ec7ba36b…` (4850); тело третьей функции перенесено в репо и сверено с продом по тому же md5. SECURITY DEFINER / `search_path=public` / гранты / SEC-12 guard сохранены, SEC-11 (anon без execute) не затронут. Семантика сброса проверена read-only выражениями (8→8, JSON null→NULL, ''→NULL, ключ absent→сохранить). Первая попытка применения упала на синтаксисе (вставка съела закрывающую `)` списка колонок) — прод не изменился, CREATE не применился (`check_function_bodies` компилирует тело), после чего в генератор добавлена проверка баланса скобок. **Не проверено на устройстве**: end-to-end «редактор → сохранить → перезаход → карточка» требует тестового аккаунта |
| RPE-2 | 🟡 | ✅ | Исторические `target_rpe` заполнены (миграция `20260925231000_backfill_target_rpe_templates.sql`, прод 25.09). Канон детерминированный, из самих данных: фаза `deload` → 6, `intensity='low'` → 9, `medium|high` → 8 (проверено: на deload ветка increase требует RPE ≤ 4, то есть ложных «поднимай вес» в разгрузку не появится). Область — шаблоны (`created_by IS NULL`) и их копии (`source_program_id IS NOT NULL`): заполнено **903** строки `program_exercises` (296×6, 398×8, 209×9) и **631** строка `workout_exercises` открытых плановых тренировок (маппинг `(phase_number, day_number, exercise_id)` проверен на 1:1 до записи; `min()` только как страховка). Идемпотентность: `WHERE target_rpe IS NULL`, поэтому ручные значения не затираются. **Сознательно не тронуто:** 34 строки `program_exercises` и 129 строк `workout_exercises` авторских программ пользователей (интенсивность выбирал человек, не куратор каталога) и 200 строк выполненных/пропущенных тренировок (историю рекомендаций нельзя переписывать задним числом). Blast radius: у владельцев копий шаблонов активируется ветка `RPE_UNDER_TARGET` (`progression.ts:604-614`) — прогрессия предлагается раньше, при записанном RPE на 2 пункта ниже целевого и повторам ≥ минимума диапазона; у тех, кто RPE не логирует, поведение не меняется. Первая попытка упала на `42P01` (в `UPDATE … FROM` нельзя ссылаться на псевдоним обновляемой таблицы внутри JOIN — вторая часть переписана подсеткой-маппингом), транзакция откатилась целиком, прод не пострадал. Не проверено на устройстве |
| RPE-3 | 🟡 | ✅ | `target_rpe` проставлен в три новые программы П1 и они выложены на прод (миграции `20260925220000`/`220500`/`221000`, каждая отдельным apply_migration). Канон тот же, что RPE-2, но тип фазы задаётся **литералом блока**, а не `join program_phases`: первая версия прочитала таблицу, в которую тот же оператор вставил фазы через CTE, — основной запрос видит снимок до начала оператора, join дал 0 строк, и «Домашняя» легла с 3 фазами и 9 днями **без упражнений** (проверено до удаления: workouts=0, копий=0; замена одним атомарным скриптом `seed_program_home_dumbbell_floor_fix`). Post-verify по всем трём: `unresolved=0`, `name_drift=0`, `no_rpe=0`, 3 дня/фазу, подходы 45/45/30 (домашняя), 52/51/34 (задняя цепь), 42/30 (дефицит), `needs_review` — ровно одна строка на фазу в «Задней цепи» («Сгибание ног лежа», в каталоге нет approved-упражнения с паттерном `knee_flexion`). Каталог: 6 → 9 готовых программ |

### Расширение каталога: пакет П2 26.09.2026 (FD-SEED-3, прод-DML по явному подтверждению пользователя)

Вердикт: каталог 9 → **12** готовых программ. Все три закрывают дыры, подтверждённые данными прода (частота, уровень, незадействованные `movement_pattern`), а не «ещё одна программа». Скилл: `.cursor/skills/program-seeding`.

| ID | Пр. | Статус | Цель |
|---|---:|---|---|
| FD-SEED-3 | 🟡 | ✅ | Три программы выложены 26.09: (1) **«Upper/Lower — Сила»** (`b4000000-…`, advanced, 4 дня Пн/Вт/Чт/Пт, 8 нед: Объём 4 → Сила 3 → Дилоуд 1, 64 строки, 71/63/42 подхода) — в каталоге не было 4-дневного advanced: существующая «Upper/Lower» intermediate, а advanced-версия одна на 6 дней; отличие — блочная периодизация со снижением повторений (6–8 → 3–4) и ростом отдыха (180 → 240 с), вектор «объём силовой фазы меньше объёмной» соблюдён (63 < 71), то есть не повторяет замечание FD-SEED-2c; (2) **«Две тренировки в неделю»** (`b5000000-…`, intermediate, Пн/Чт, 12 нед: База 6 → Сила 5 → Дилоуд 1, 36 строк, 38/35/24) — минимальная частота в каталоге была 3 дня; в description прямо названа программой удержания, а не роста (3–4 подхода на движение = нижняя граница дозы); (3) **«Атлетизм: прыжок и бросок»** (`b6000000-…`, intermediate, 4 дня, 8 нед: Техника 4 → Мощность 3 → Дилоуд 1, 63 строки, 66/63/42, плио 14/16/8) — первая программа каталога с паттернами `jump`/`throw`/`rotation`: до неё они не использовались ни в одной из 9 (в каталоге упражнений: jump 17 approved, throw 13, rotation 41). Плио выражена короткими подходами 3–6 повторов с длинным отдыхом, потому что концепции «контактов» нет (FD-SEED-2a). Имена брались из `exercises` и проверялись по байтам файлов: 42 уникальных имени, `unresolved=0`, кратность в каталоге 0, все `status='approved'` (в отличие от П1 — ни одной `needs_review`); `target_rpe` по канону RPE-2 (deload 6 / low 9 / база 8) во всех 85+36+84 строках. Post-verify (прод): по фазам 4/4/4 и 2/2/2 дня, `bad_days=0`, `unresolved=0`, `name_drift=0`, `no_rpe=0`; press:pull 1.00/1.20/1.00, 1.00/1.00/1.00, 1.00/1.11/1.00 — все в коридоре 1.0–1.2, `post_chain ≥ squat` кроме фазы «Сила» в UL (12 против 10 — норма). **E2E под ролью `authenticated`** (в `begin; … rollback;`, через `copy_program_for_user` на реальном uid): копия «UL — Сила» = 3 фазы / 12 дней / 64 упражнения / 64 с `target_rpe` / 0 дней без `phase_id` — то есть и копирование, и цепочка RPE на новых данных работают |
| FD-SEED-3h | 🟡 | 🔲 | Технический долг выкладки: payload-лимит на один вызов `apply_migration` заставил класть каждую программу тремя отдельными миграциями (`…`, `…_phase2`, `…_phase3`) — в проде 9 history-записей против 3 файлов в `supabase/migrations/`. Данные идентичны файлам (сверено построчно post-verify), но «одна миграция = одна программа» сломано; чинится либо переносом пайплайна на `supabase db push` (нет `config.toml`, нет токена — см. память reference-supabase-mcp), либо уменьшением seed-файлов |

### Верификация 6 функций 24.09.2026 (чтение кода + сверка с прод-БД: колонки таблиц, тело RPC `upsert_workout_logs`)

| ID | Пр. | Статус | Цель |
|---|---:|---|---|
| VF-P0 | 🔴 | ✅ | Исправлены три критичных дефекта: (1) `isSetCompleted` считал пустые сеты завершёнными — маппер создавал сеты без ключей `reps_left/reps_right` (`undefined !== ''`) → на свежих тренировках не рендерился RecommendationCard, RPE-чипы лезли в пустые сеты, отдых стартовал на маунте, «✓ N/N» врал — инициализация пустых строк в mapper + guard `?? ''` в предикате; (2) `useBodyMetrics.createMutation` терял `thigh_left_cm/thigh_right_cm` (FEAT-2.3 сломан end-to-end: форма собирает, в БД NULL) — маппинг добавлен; (3) quick-set готовности из pip StatusCard затирали `sleep_hours/sleep_quality/fatigue/soreness/stress` — `upsertToday` теперь update'ит только ненулевые поля (patch-семантика). Gates: tsc ✅, eslint ✅. **Шаринг тренировки как функции не существует** — единственный `Share.share` шарит программу, он после SEC-12 проверен и консистентен |
| VF-P1 | 🟠 | ✅ | Находки верификации 24.09 — исправлены 25.09 (9/10, проверено повторным чтением кода перед правками): (VF-1) ✅ early-return `updateSetFeedback` сравнивал только rpe/rir/difficulty → чип «Разминка» на пустом сете отбрасывался; предикат теперь покрывает isWarmup/estimatedReps; (VF-2) ✅ select `recentLogs` получил `reps_left/reps_right`, previous строится через `effectiveReps` — рекомендации вернулись для unilateral; (VF-3) ✅ `flushPendingLogs` возвращает число неуспешных групп и возвращает их в pending (без затирания свежих данных); `saveWorkout` при ошибке НЕ пишет `finished_at` и просит повторить; (VF-4) ✅ `addSet` стал пакетным `(index, count)` — один state-update и один persist на warmup toggle; `addSet`/`updateExerciseSettings` двигают `target_sets` локально и персистят через новый `updateWorkoutExerciseTargetSets` (та же таблица/грант, что у ENG-16) — авто-добавленные разминочные сеты больше не исчезают после перезахода; (VF-5) ✅ уже была исправлена ранее (select несёт `is_estimated_reps`, mapper восстанавливает `estimatedReps`, flush пересчитывает флаг); (VF-6) ✅ `getPersonalRecordsWithDates` получил канонический FD-7 фильтр — ложные PR из незавершённых/пропущенных сессий устранены; (VF-7) ✅ `rowsToMuscleLoad` — дедуп front∩back через `new Set` (канон muscleLoad.ts), трапеция больше не в двойном счёте; (VF-8) ✅ ~12 мест вывода в аналитике переведены на единицы: RecentWorkouts, ProgressStats, VolumeTrendChart (итог + бары), StrengthTrendChart (MiniLineChart: первый замер + дельта), MuscleStatsSection (топ e1RM), TrainingCalendarCard (объём + лучшая), WeightTrendRow, WeightTrendChart (slope/last/Δ/min/max, без roundToHalf — история замеров не искажается); **остатки VF-8 (осознанные)**: ввод «Текущий вес (кг)» в GoalsStep1 и «вес грифа (кг)» в настройках — единицы хранения, не вывод; мёртвые `PersonalRecordsCard.tsx`/`LastWorkoutCard.tsx` не чищены (кандидаты на удаление); (VF-9) ✅ чек-ины (StatusCard quick-set, ReadinessSheet) инвалидируют `['todayRecovery']` — движок видит свежий сон/стресс сразу; (VF-10) ✅ дефолт `defaultDate` в `CycleCheckInSheet` — локальный `todayKey()` вместо UTC `toISOString()`. Gates: tsc ✅, eslint ✅. Не проверялось на устройстве (нет UI-регресса) |

### UX-дизайн: пакет UX-2 «Честная загрузка» 25.09.2026 (spec: spark-output/specs/UX-2-loading.md)

Вердикт: экраны перестают врать и «висеть». Gates: `tsc --noEmit` ✅, `eslint` (изменённые файлы) ✅, `expo export --platform android` ✅. **Device-проверка не сделана** (тот же блокер — тестовый аккаунт): shimmer-плавность, anti-flash на быстром интернете, PTR-жест.

| ID | Пр. | Статус | Цель |
|---|---:|---|---|
| UX-L1 | 🟠 | ✅ | audit-11/13 (+L-1/L-2): новые `ui/skeletons.tsx` (DashboardSkeleton по макету: greeting/streak/insight/hero/2-ряд/календарь; ProgramListSkeleton; ProgressHeroSkeleton+StatsGridSkeleton; WorkoutSkeleton) под новым `ShimmerWrap` (один SharedValue на контейнер, white-блик 14°). Dashboard/Programs/Progress/Workout перешли с generic `ListSkeleton` на макетные |
| UX-L2 | 🟠 | ✅ | audit-4/12: новый `ui/StateBlock.tsx` (иконка-круг + заголовок + «что делать» + CTA) — Dashboard error/no-auth, programs/exercises error, **новая screen-level ошибка тренировки** (`loadError` в `useWorkoutSession`, раньше провал тонул в Alert при пустом списке; retry = `loadWorkout()`) |
| UX-L3 | 🟡 | ✅ | audit-13 + L-3: PTR на Dashboard (`Promise.allSettled` dashboard+history+weekly+nutrition); `useMinPending(250ms)` anti-flash на 4 экранах; stagger-вход секций Dashboard через `FadeIn delay 0…360` (монтируется один раз после skeleton — рефетчи не анимируются); nutrition-spinner → точечный skeleton |
| UX-L4 | 🟠 | ✅ | audit-14 (замечание пользователя): `MuscleLoadModeToggle` приведён к масштабу `PillToggle` (typography.label, minHeight 44, паддинги SPACING) — чинит все 3 использования разом; PillToggle-замена отклонена (ломает компактные раскладки ProgramMuscleMap/WorkoutReport) |

Откат — один revert-коммит. Остаток по UX-2: cross-fade карты мышц (I-7, взят в отдельный мини-шаг) и UX-2b (persisted queries) — не сделаны.

### UX-дизайн: пакет UX-1 «Движение» 25.09.2026 (spec: spark-output/specs/UX-1-motion.md; находки: spark-output/audit/fittracker-rn.md)

Вердикт: «топорность» лечится на уже установленном стеке (Reanimated 4 / react-native-screens), без новых зависимостей. Gates: `tsc --noEmit` ✅, `eslint` (все затронутые файлы) ✅, `expo export --platform android` (бандл) ✅. **Не проверено на устройстве** (блокер — тестовый аккаунт, см. память 25.09): smoothness переходов, gestureEnabled-свайп на iOS-риалайте, дребезг при быстрых тапах FAB.

| ID | Пр. | Статус | Цель |
|---|---:|---|---|
| UX-M1 | 🟠 | ✅ | audit-5: управляемые переходы root-Stack (`app/_layout.tsx`): дефолт `slide_from_right` для drill-in, `fade` для `(auth)`/`(tabs)`, `exercise/[id]` — modal + `slide_from_bottom` + `gestureEnabled` (жест-альтернатива закрытия) |
| UX-M2 | 🟠 | ✅ | audit-6: новый канон-примитив `src/components/ui/PressableScale.tsx` (Pressable + createAnimatedComponent, spring scale 0.97…0.985 + лёгкий opacity-dim + опц. хаптика; один узел — absolute-FAB и вложение в FlashList безопасны). Мигрированы горячие точки: AppButton, AppCard(onPress), ProgramCard, WorkoutListItemCard, FAB programs, ExerciseRow. Остальные ~600 TouchableOpacity — пакет UX-4 (поэкранно) |
| UX-M3 | 🟠 | ✅ | audit-8: SheetShell получил `animateEnter`, по умолчанию `!isModal` — внутри нативного Modal собственный slide-up больше не «резинит» с native-анимацией (чинит все 12 Modal-host'ов одной правкой); exit-анимация сохранена (ранний unmount показал бы пустую шторку) |
| UX-M4 | 🟡 | ✅ | audit-9: CustomTabBar — pill на `withSpring` (первый layout без анимации), scale-pop иконки при фокусе; отдельный мемоизированный `TabIcon` |
| UX-M5 | 🟡 | ✅ | I-3: RestTimer — градиентная обводка кольца из `gradients.primary` (finish — сплошной success), тикающий light-хаптик в последние 3с, вспышка scale 1.06 + одиночный success-хаптик в `isFinished` (цикл vibrateUntilDismissed не конфликтует) |
| UX-M6 | 🟠 | ✅ | Фризы (замечание пользователя 25.09, после UX-1/UX-2): (1) `freezeOnBlur:true` на root-Stack и Tabs — unfocus-экраны не живут в JS во время переходов; **workout/[id] исключён** (`freezeOnBlur:false`), т.к. resting-таймер на JS-интервалах останавливался бы под модалкой; (2) PressableScale — убрана анимация opacity (scale-only): прозрачность родителя с elevation-детьми = offscreen-raster каждый кадр на Android; (3) ShimmerWrap — убран `rotate('14deg')` поворотного слоя (то же), наклон имитирован диагональными start/end градиента; (4) FadeIn — новый `fade={false}` (translateY-only), все 7 stagger-секций Dashboard переведены: вход без альфы, теные карточки не пересобираются. Gates: tsc ✅ eslint ✅ export ✅. **При приземлении shimmer/переходов перепроверить на release-сборке**: в dev-режиме (expo start) JS-тред сам по себе медленный — часть «микрофризов» может быть артефактом dev-бандла |
| UX-H1 | 🟠 | ✅ | UX-1h (I-4): hero-морфинг Dashboard→тренировка без новых зависимостей — `ui/HeroMorph.tsx`: `HeroOut` (transform-only scale 1.05 на карточке «сегодня», navigation через 140ms, ref-guard от даблтапа) + `HeroIn` (header тренировки: scale 0.9→1 + fade, 120ms delay; включается только параметром маршрута `?hero=1`, который прокидан Dashboard → `workout/create` (fade-экран) → `workout/[id]`). Обычные входы в тренировку (из «Тренировок», repeat) анимацию НЕ играют. Gates: tsc ✅ eslint ✅ export ✅; device — не проверено |
| UX-B1 | 🟠 | ✅ | UX-2b (L-4): persisted queries — `src/lib/queryPersistence.ts` + `@tanstack/react-query-persist-client`/`query-async-storage-persister` (v5.104, мажор равнен react-query). Холодный старт: restore ДО релиза splash-гейта → экраны получают данные мгновенно, skeleton не мигает. Инварианты приватности: per-user ключ `FTQ::<uid>`; SIGNED_OUT → unsubscribe + **erase** blob; login на смонтированном дереве → attach + `invalidateQueries()` (stale-while-revalidate, без залипания устаревших данных); порядок restore→subscribe ядный (иначе догнавший restore затрёт свежие ответы); дегидрируются только `status==='success'` read-запросы; maxAge 24ч + buster. Осознанный остаток: AsyncStorage не шифрован (метрики владельца девайса) — сужается allow-list'ом при требованиях. Gates: tsc ✅ eslint ✅ export ✅; device — не проверено (главный сценарный тест: kill+restart → экран без skeleton; logout → вход вторым аккаунтом → чужих данных не мелькнуло) |
| UX-T1 | 🟠 | ✅ | UX-3 часть «шрифт» (audit-1, выбор пользователя 26.09): пара **Space Grotesk (display) + Inter (text)** через `@expo-google-fonts/*` (7 весов). Токены — `src/constants/fonts.ts` (`FONT_FAMILIES` + `APP_LOADABLE_FONTS`), загрузка в корне до релиза splash-гейта (`useFonts`), владельческий слой — `typography.ts`: h1–h5 → Space Grotesk (700/600), body/caption/label/button/overline → Inter (400/500/600/700); крупнейшие инлайн-числа (RestTimer 48/56) получили displayBold напрямую; ProgressStats и др. покрыты через h3-токен. Важно: RN не синтезирует bold из custom-font → инлайновые `fontWeight` без fontFamily меняют **только системный fallback**, вес кастомного шрифта задаётся именем файла (ограничение зафиксировано комментарием в fonts.ts). Gates: tsc ✅ eslint ✅ export ✅ (ttf попали в бандл); device — не проверено (приглядеться к цифрам в WeightTrendChart/Timers: там инлайны без токена). Остаток UX-3: SHADOWS-токены (audit-2), count-up (I-1), PR-glow (I-5), L-5 брендовый лоадер, L-8/I-8 тема-онбординг |

Откат — один revert-коммит, правки чисто UI. Следующие пакеты: UX-2 «Честная загрузка» (включая audit-14 — рассинхрон MuscleLoadModeToggle/PillToggle), UX-2b persisted queries (требует явного approval инвариантов приватности), UX-3 «Идентичность» (шрифт — ждёт выбора), UX-1h hero-морфинг.

## 5. Training Engine

| ID | Пр. | Статус | Цель |
|---|---:|---|---|
| ENG-1 | 🔴 | ✅ | формализовать progression rules — выполнено: src/engine/progression.ts, 8 правил (increase/hold/decrease/no_data), structured reason codes, live recompute в SetsGrid, chip highlight; фундамент для ENG-2 и COACH-1 |
| ENG-2 | 🔴 | ✅ | structured reasons для recommendations — выполнено: explainProgression (input/signal/conclusion факты по reason.code), тап по recommendation → inline-блок «Почему?» + «Скрыть» (session-local); детерминированно без LLM (ROADMAP B5); persistence причин — COACH-3 |
| ENG-3 | 🔴 | ✅ | readiness как optional signal — выполнено: applyReadinessContext (readiness 1–2 + base increase → downgrade до hold, LOW_READINESS; null = no-op по PRODUCT.md §7); применяется после applySafetyPrecedence (PRODUCT.md §8); useTodayReadiness (React Query, 1h); проброс workout/[id] → ExerciseSlider → ExerciseCard → SetsGrid; «Почему?» расширен readiness-сигналом |
| ENG-4 | 🔴 | ✅ | safety precedence: pain/injury > recommendation > AI — выполнено: applySafetyPrecedence (4 правила: PAIN_STOPPED/INJURY_AVOID suppress → no_data; PAIN_RECORDED/INJURY_CAUTION downgrade increase → hold); visual: safety-downgrade в warning color, chip highlight выключен; engine никогда не обходит injury_exercise_warnings (PRODUCT.md §8) |
| ENG-5 | 🔴 | ✅ | ranking exercise alternatives — выполнено: engine/alternatives.ts rankAlternatives (hard exclusion: avoid-противопоказания при совпадении с травмой + targetsInjuredMuscle severity high; scoring: мышцы +2/+1, pattern +3, оборудование +2/−1, уровень −3/−2, боль в группе −3, injury medium/low −5/−2; relation-type: regression +5 при боли/травме, −1 без, progression −3 при боли); fetchAlternatives: movement_pattern/difficulty в select + relationTypeMap; ExerciseSlider: подпись «N скрыто из-за травм» + all-excluded state; AlternativeExerciseCard: бейджи Прогрессия/Упрощение/Вариант; данные: бэкфилл 20260819 |
| ENG-6 | 🟠 | ✅ | deterministic weekly summary — выполнено (data layer): engine/weeklySummary.ts (buildWeeklyInsights, 9 детерминированных правил), weeklySummaryService.getWeeklySummary (3 параллельных запроса + pre-week PR query), useWeeklySummary (React Query, 5 min); без LLM (ROADMAP C4); UI — COACH-5. **Добавлен расчёт ACWR (Acute:Chronic Workload Ratio) в calculateTrainingLoadContext для оценки риска травм (sweet spot 0.8–1.3, >1.5 warning).** |
| ENG-7 | 🟠 | ✅ | P0 Formula Enhancements: **убраны жёсткие эвристики RPE по возрасту/фарме** (заменяют логику прогрессии, что противоречит PRODUCT.md §8). Вместо этого используются универсальные безопасные пороги (RPE ≤ 7 для прогресса, ≥ 9 для hold), а контекст (возраст, усталость) учитывается через сигналы SESSION_FATIGUE/SESSION_LIGHT_DAY и explainability. Плато 3+ недели → deload -10%, isDeloadWeek → -10%. Все параметры опциональны и берутся из профиля пользователя. |
| ENG-8 | 🟠 | ✅ | P1.1 КБЖУ через сухой вес: формула Кэтча-МакАрдла (370 + 21.6 × сухой_вес) в macroCalculator.ts; UI — toggle «Знаю свой % жира» + раскрывающееся поле в GoalsStep1 (валидация 1–60), сохранение в profiles.body_fat_percentage; GoalsStep3 показывает формулу Кэтча-МакАрдла в «Как рассчитано». Fallback на Миффлин-Сан Жеор при null. **Критическое исправление безопасности (PRODUCT.md §14): удалены опасные фармакологические множители (3 г/кг белка, 0.8 г/кг жиров). Добавлены научно обоснованные потолки: белок max 2.4 г/кг (MPS plateau), жиры min 1.0 г/кг (гормональный минимум).** |
| ENG-9 | 🟠 | ✅ | P1.2 Периодизация в программах: добавлены currentPhaseType и weeksInBlock в progression.ts. Deload фаза, **авто-deload при >6 недель во всех ветках прогрессии** (исправлено: ранее срабатывало только при allAtMax + RPE ≤ 7), корректировка шага (strength 2.5, hypertrophy 1.25, endurance 1.0). |
| ENG-10 | 🟠 | ✅ | P1.3 Раздельный учёт типов тренировок: добавлен volumeByType в weeklySummary.ts. Разные пороги объёма (силовая ±15%, гипертрофия ±10%, кардио ±20%) и инсайт TYPE_IMBALANCE. Требует SQL-миграции для поля workout_type. |
| ENG-11 | 🟠 | ✅ | P2.1 E1RM для high-rep сетов: переключение формул в e1rm.ts (reps ≤ 10 → Epley, reps 11–12 → Brzycki, **reps > 12 → Wathan**). Формула Wathan предотвращает завышение 1ПМ на 10–20%, которое дают Epley/Brzycki при мышечной выносливости (>12 повторений). |
| ENG-12 | 🟠 | 🔲 | ~~P2.3 Прогрессия весов в разминке~~ — **отметка снята 25.09 (FD11-8)**: `generateWarmupSets` существовала, но не вызывалась нигде (проверено grep'ом); мёртвый код удалён из warmupService.ts по решению пользователя. Прогрессия весов в разминке НЕ реализована; протокол blood-flow (40%×12, 60%×6) / compound (50×5, 70×3, 85×1, 100×1) сохранён здесь как спецификация для будущего подключения к WarmupBlock |
| ENG-13 | 🔴 | ✅ | Per-set recommendation + warmup chip + current-session context + analytics filtering: `targetSetIndex` в progression.ts (base = previousWeight целевого сета, работает для пирамид); `isWarmup` сеты исключены из оценки; усталость оценивается по завершённым рабочим сетам текущей сессии (reps/RPE/вес, а не previousReps); новые сигналы SESSION_FATIGUE (текущий вес <90% от прошлого → decrease до текущего уровня) и SESSION_LIGHT_DAY (90–100% → hold без повышения). UI: чип «Разминка» под RPE в SetsGrid; при toggle on автоматически добавляются N новых пустых рабочих сетов в конец, где N = текущее количество разминочных подходов, чтобы сохранить исходное количество рабочих подходов (пример: было 4, пометил 1 как разминку → +1 новый = 5 всего; пометил 2 → +2 новых = 6 всего). Schema: `workout_logs.is_warmup` + `is_estimated_reps` (миграция 20260827180609_workout_logs_is_warmup). Analytics filtering (закрыто): все сервисы (historyService, progressService, weeklySummaryService, dashboardService, exercisesServiceNormalized, profileService) фильтруют `is_warmup=false` при агрегации volume/sets/avg_rpe/PR/e1RM/muscle volume; `recentLogs` (previous data для прогрессии) тоже фильтруется по `is_warmup=false` в workoutService.ts. Workout Report (getWorkoutDetail) возвращает `is_warmup` в `WorkoutDetailLog` для UI маркера. Остаток: регенерация `database.types.ts` после применения миграции (колонка `is_warmup` + `is_estimated_reps`). **Багфикс**: новые сеты (через `addSet` / `updateExerciseSettings`) теперь наследуют `previousWeight`/`previousReps`/`previousRpe` из последнего сета с историей, что гарантирует корректную работу рекомендаций для динамически добавленных подходов. |
| ENG-14 | 🟠 | ✅ | P0 Recovery Context (Сон/Стресс, Вариант B): добавлены поля `sleep_hours`, `sleep_quality`, `stress` в `daily_readiness` (миграция 20260828061945_daily_readiness_recovery_details). В `progression.ts` добавлены правила `LOW_SLEEP` (<6ч) и `HIGH_STRESS` (≥4), блокирующие повышение веса. Авто-расчёт readiness на основе деталей в `readinessService.ts`. L1 чипы сна/стресса в `StatusCard` с warning-цветом при отклонениях. L3 тренды восстановления через `useRecoveryTrend` **интегрированы в `app/profile/metrics.tsx`**. |
| ENG-15 | 🟠 | ✅ | P1 Menstrual Cycle (Balanced): добавлены таблицы `cycle_events` и `cycle_settings`. В `progression.ts` добавлены правила `LUTEAL_PHASE` и `OVULATION_RISK`. Чистые функции `calculateCyclePhases` и `getPhaseForDate` в `utils/cycle.ts`. Единый flow: L2 `CycleCheckInSheet` вызывается из `ReadinessSheet` (под блоком стресса) или по тапу на день календаря в режиме правки. L2: `CycleCalendar` (рассчитывает фазу для каждого дня) и `CycleSettingsSheet` (по шестерёнке) в `profile.tsx`. Используется поле `gender` из раздела «Мои цели» (колонка `sex` упразднена). Удалён прямой `supabase.from` из UI (CLAUDE.md §2). |
| ENG-16 | 🔴 | ✅ | Fix progression & safety pollution on temporary replacement: update `workout_exercises.exercise_id` AND `pain_events.exercise_id` in DB during temp replace. Ensures logs and pain context are attributed to the actually performed exercise, preventing lost injury warnings and false progression recommendations. |
| ENG-17 | 🔴 | ✅ | Фича 2: RPE-based Autoregulation. Добавлено поле `target_rpe` в `program_exercises` (миграция 20260904). Движок `progression.ts` теперь учитывает целевой RPE: если фактический RPE ≤ целевого - 2 и повторы в диапазоне, предлагается прогрессия. UI: `ExerciseSettingsSheet` позволяет задать целевой RPE. **Уточнение 25.09:** end-to-end фича не работала — три серверных write-пути (`save_program_snapshot`, `copy_program_for_user`, `create_workouts_for_program`) теряли поле, в проде было 0 значений; закрыто в разделе RPE-1 выше. Статус ✅ относится к движку и UI, а не к сохранению.
| ENG-18 | 🟠 | ✅ | Фича 4: Pain Trend by Body Part. Чистая утилита `calculatePainTrend` в `utils/painTrend.ts` группирует `pain_events` по `body_part` и ISO-неделям (окно 4 недели назад). Зоны с болью в ≥2 разных неделях — хронические. Сервис `painService.getPainEventsInRange`, хук `usePainTrend` (staleTime 5 мин). UI: `StatusCard` показывает кликабельный error-чип "Боль: {зона} · {n} нед." (вместо warning "Боль сегодня") с `PainTrendSheet` (L2: таблица недель + рекомендация без медицинского диагноза); `ProgressInsights` (Progress hub) добавляет observation "Устойчивая боль" в начало списка инсайтов. |
| ENG-19 | 🟠 | ✅ | **Фича 7: Next Workout Forecast**. Детерминированный прогноз сложности следующей тренировки (easy/normal/hard) на основе реальных объёмов последних 4 недель. Чистая функция `calculateWorkoutForecast` в `utils/workoutForecast.ts` (thresholds 0.85/1.15, insufficient-data guard при < 3 тренировках за окно — честный fallback, PRODUCT.md §14). Сервис `forecastService.getWorkoutForecast` (единственная supabase-граница, 2 параллельных запроса, без N+1): активная программа → следующая тренировка → workout_exercises → workout_logs + recent workout volumes. Хук `useWorkoutForecast` (staleTime 5 мин). UI: L1-бейдж «Тяжёлая/Лёгкая/Обычная» в Sticky-карточке `workouts.tsx` (рядом с названием); L1-чип «Следующая: тяжёлая» в `StatusCard` (Dashboard); L2 — `WorkoutForecastSheet` с разбивкой по упражнениям, сравнением с средним объёмом тренировки и disclaimer «наблюдение, а не предписание». |

## 6. Coaching Layer

| ID | Пр. | Статус | Цель |
|---|---:|---|---|
| COACH-1 | 🔴 | ✅ | Recommendation card: Accept / Change / Why — выполнено: `RecommendationCard` в `SetsGrid`: suggested weight × reps как primary, «Принять»/«Изменить» как secondary, collapsible «Почему?» как tertiary; progression chips скрыты до «Изменить»; Accept пишет suggestedWeight/suggestedReps в первый незавершённый сет через существующий `updateSet`; «Скрыть» session-local без persistence; safety/readiness overrides используют warning color и не подсвечивают increase-chip. Причина изменения/отклонения и feedback — COACH-3| 
| COACH-2 | 🔴 | ✅ | sstructured reasons — реализовано в рамках ENG-2 (explainProgression + «Почему?»/«Скрыть» в SetsGrid)|
| COACH-3 | 🟠 | ✅ | acceptance/rejection feedback — выполнено: таблица recommendation_feedback (upsert по user+workout+exercise+set), recommendationFeedbackService, useRecommendationFeedback (fire-and-forget useMutation, ошибки глотаются тихо); UI в SetsGrid: после «Скрыть» inline-чипы причин (устал/слишком тяжело/боль/хочу легче/другое) + пропустить (×); «Принять» записывает accepted без причины; PRODUCT.md §3.2 L2 inline-фидбек без sheet/modal; фактический вес при ручном изменении (changed) — вариант B отложен |
| COACH-4 | 🟠 | ✅ | contextual tips без спама (Dashboard L1 + readiness warning) |
| COACH-5 | 🟠 | ✅ | weekly review UI (Progress hub, deterministic insights) — закрыто через CI-1 |


### Coaching Intelligence — planned

> Базовый deterministic weekly summary (ENG-6 / COACH-5) уже реализован. Ниже — следующий этап: расширить существующие данные до полноценного explainable Coaching Intelligence. Полные функциональные спецификации и зависимости — `ROADMAP.md`, §4 C5–C12.

| ID | Пр. | Статус | Цель / зависимость |
|---|---:|---|---|
| CI-1 | 🔴 | ✅ | Weekly Training Review: consistency + performance + load + recovery/safety context + explainable next-step context; расширяет baseline ENG-6/COACH-5. UI реализован в Progress hub (WeeklyReviewSection + SheetShell). |
| CI-2 | 🔴 | ✅ | Training Load Context: normal/elevated/high на основе volume/frequency/RPE/readiness trends + **ACWR (4-недельное скользящее среднее)** для оценки риска травм; deterministic, без opaque score. UI интегрирован в WeeklyReviewSection (L1/L2). |
| CI-3 | 🟠 | ✅ | Plateau Detection: L2-блок с observation и вариантами действий при PLATEAU_DETECTED (ROADMAP C8) |
| CI-4 | 🟠 | ✅ | Muscle Volume Analysis: weekly sets, распределение по мышечным группам, тренды и explainable imbalance signals (L2-блок в WeeklyReviewSection). **P2**: расширено до 3D Muscle Map (Balance / Fatigue с затуханием / Strength с 1RM) с переключателем режимов. |
| CI-5 | 🟠 | ✅ | Goal-aware Insights (Вариант B: Balanced): цель из профиля (`profiles.goal`) учитывается в `buildWeeklyInsights` — приоритеты инсайтов и контекстная адаптация текста («почему это важно для твоей цели»). Чистая логика в `engine/weeklySummary.ts`, интеграция в `weeklySummaryService.ts`, UI доверяет отсортированному массиву. Без дублирования экранов. |
| CI-6 | 🟡 | ✅ | Deload Recommendations + **Фича 5: Technique Week** — выполнено (Вариант B, ROADMAP C11): `calculateDeloadContext` в `engine/weeklySummary.ts` (4 объяснимых сигнала: highLoad/plateau/readinessDecline/rpeRisingNoImprovement; рекомендация при ≥3 сигналах или highLoad+plateau/readinessDecline; insufficient-data guard для первой недели; `DeloadContext.availableTypes: ['volume', 'technique']` при `recommended: true`); `WeeklyReviewSection` L1-карточка с Moon/warning + **2 кнопки выбора стратегии (Объём/Техника)** + session-local dismiss; L2 SheetShell содержит два независимых блока: «Разгрузочная неделя (Объём)» (−40–60% объёма, RPE ≤ 6–7) и «Техническая неделя» (вес 65–70% от рабочего, акцент на идеальную технику, RPE 6–7, сохранение количества подходов — восстановление ЦНС без потери навыка) с disclaimer «не меняет программу автоматически»; без автоизменения программы (PRODUCT.md §3.3 user control) |
| CI-7 | 🟡 | 🔲 | Optional AI Explanations: AI объясняет structured facts/reason codes; зависит от Coaching Intelligence и AI foundation |

Порядок реализации:

```text
CI-1 → CI-2 → CI-3 / CI-4 → CI-5 → CI-6 → CI-7
```

`CI-3` и `CI-4` могут выполняться параллельно после стабилизации CI-2. `CI-7` не является условием ценности базового продукта.

## 7. Programs / Program Editor

| ID | Пр. | Статус | Цель |
|---|---:|---|---|
| PROG-1 | 🔴 | ✅ | аудит каталога готовых/личных программ: sort menu → SheetShell, copyProgramForUser вынесен в programsService, LayoutAnimation убран (CLAUDE.md §9 anti-pattern), empty state ready-tab — кнопка «Импортировать по коду» |
| PROG-2 | 🔴 | ✅ | аудит Program Card: бейджи (Моя/Уровень/Длительность/Текущая), цветная полоса уровня, акцентная рамка для активной, расписание, footer с Edit/Активировать/Подробнее; Alert для ready-programs сохранён (деталь экрана не имеет CTA «Скопировать») |
| PROG-3 | 🔴 | ✅ | разделить Program Detail и Editor: создан отдельный маршрут `/program/[id]/edit`, убран toggle `editMode` из Detail, вынесены editor-модалки в `ProgramEditorModals` |
| PROG-4 | 🔴 | ✅ | вложенность программы: карточки упражнения получили собственный контейнер и явную L1-схему (подходы × повторы пилюлей); визуальный шум снижен |
| PROG-5 | 🔴 | ✅ | progressive disclosure в Program Editor: детали упражнения (отдых/интенсивность) — L2 через sheet, на карточке только схема и мышцы |
| PROG-6 | 🟠 | ✅ | context/breadcrumb: добавлен компактный breadcrumb в шапку экрана редактирования |
| PROG-7 | 🟠 | ✅ | sheets + save/sync UX: убран блокирующий `Alert`, добавлен спокойный `Toast`; все editor-листы переведены на каноничный `SheetShell` (INVENTORY.md §6) |
| PROG-10 | 🔴 | ✅ | **Fix modal clipping (AUDIT-1)**: модалки редактора программы обрезались/смещались вниз. Причина: в New Architecture (Fabric) использование `position: 'absolute'` (поведение `SheetShell` по умолчанию) внутри нативного `<Modal>` приводит к багу сжатия layout. Решение: проп `isModal` **добавлен** во все `SheetShell` внутри `<Modal>` (`ProgramEditorModals`, `ProgramDetailModals`, `NutritionAddModal`). Это переключает компонент на использование `flex: 1` внутри `KeyboardAvoidingView`, что является каноничным и надёжным способом занять весь экран модалки. Задокументировано в INVENTORY.md §12.
| PROG-8 | 🟠 | ✅ | Dirty tracking + exit guard + phase drag & drop: `isDirty` в `useProgramEditor` (deep-equal через JSON.stringify + проверка deleted IDs), Alert при выходе с несохранёнными изменениями (BackHandler + useFocusEffect), индикатор-точка рядом с «Сохранить», фазы перетаскиваются через `DraggableFlatList` (консистентность с днями/упражнениями), убраны ChevronUp/ChevronDown из PhaseCard (заменены на GripVertical drag handle) |
| PROG-9 | 🔴 | ✅ | Drag/scroll conflict fix в Program Editor: мигрировали на официальный nesting pattern библиотеки — outer `ScrollView` (RN) → `NestableScrollContainer` (RNGH ScrollView), все три `DraggableFlatList` (фазы/дни/упражнения) → `NestableDraggableFlatList`. React context координирует жесты: `setOuterScrollEnabled(false)` автоматически вызывается внутри `NestableDraggableFlatList` во время drag, предотвращая перехват outer scroll; `outerScrollOffset` shared value для autoscroll. Grip handles: `onLongPress` + `delayLongPress={150}` + `disabled={isActive}` + `accessibilityLabel`/`accessibilityRole`. Grip дня вынесен из внешнего `TouchableOpacity` (устранён nested touchable конфликт). Компромисс PRODUCT.md §3.2: drag handle с discoverable affordance достаточен, tap-alternatives для reorder не требуется |

## 8. Optional AI Coach

| ID | Пр. | Статус | Цель |
|---|---:|---|---|
| AI-1 | 🟡 | 🔲 | server/Edge llm-proxy + consent + PII filter |
| AI-2 | 🟡 | 🔲 | уточнение нагрузки поверх Training Engine |
| AI-3 | 🟡 | 🔲 | Explain / Analyze |
| AI-4 | 🟡 | 🔲 | optional Coach chat |
| AI-5 | 🟢 | ⏸️ | AI-assisted program generation после стабилизации Editor |

## 9. Release / Quality

| ID | Пр. | Статус | Цель |
|---|---:|---|---|
| REL-1 | 🟢 | 🔲 | production build (отложено) |
| REL-2 | 🟢 | 🔲 | Sentry (отложено) |
| REL-3 | 🟢 | 🔲 | tests чистых функций (отложено) |
| REL-4 | 🟢 | 🔲 | smoke/regression workout/program flows (отложено) |
| REL-5 | 🟢 | 🔲 | UX/performance profiling после изменений (отложено) |

## 10. Conscious backlog

Пока не делать главным приоритетом:
- обязательный AI chat;
- AI-generated program как основной onboarding;
- автоматическое молчаливое изменение нагрузки/программы;
- social;
- отдельную bottom-tab для каждого типа аналитики;
- сложные новые workout-сущности до стабилизации core tracker UX;
- HealthKit/Google Fit до подтверждения core retention.

## 11. Active feature branches

Активные WIP-ветки с работой, ещё не смерженной в `main`. Детали по задачам — в соответствующих секциях §4–§9; здесь — сводка.

| Ветка | Область | Основные ID |
|---|---|---|


После merge ветки строка удаляется; соответствующие ID в §4–§9 закрываются.

## 12. Performance metrics

Baseline — после первого замера (REL-5 / PERF-9). Любая оптимизация начинается с измерения, а не с предположения.

| Метрика | Цель | Текущее |
|---|---|---|
| Dashboard cold start → interactive | baseline после первого замера | не измерено |
| Mount workout screen (`workout/[id].tsx`) | baseline после первого замера | не измерено |
| Set logging (tap → save) | no dropped frames | не измерено |
| Scroll списка упражнений (pagination 40/page) | no dropped frames | не измерено |

## 13. Open technical debt (design / performance)

| ID | Пр. | Статус | Цель |
|---|---:|---|---|
| PERF-8 | 🟠 | 🔲 | Baseline-метрики workout screen и logging (см. секцию 12) |
| PERF-9 | 🟡 | ✅ | библиотека упражнений: основной список переведён на @shopify/flash-list 2.x (ROADMAP I3) |
| PERF-10 | 🟡 | 🔲 | Аудит React Query `staleTime` / `gcTime` и N+1 в загрузчиках workout/history |
| DS-1 | 🟠 | ✅ | Аудит design system завершён: шкала типографики/spacing/состояния проверены. Контраст textTertiary исправлен (WCAG 2.1 AA), добавлен fontScale и accessibilityRole/Label в AppButton и ProgramCard (Этап H4) |
| DS-2 | 🟠 | ✅ | Bottom Tab Bar: внедрён паттерн Pill Highlight (явный active state: `colors.primary` background + filled icon + bold text), haptics, `accessibilityRole="tab"`. 6 табов скомпактизированы (`inset: 2`, `paddingVertical: SPACING.sm`) для предотвращения переноса текста. Соответствует PRODUCT.md §3.1–3.2. |
| DS-3 | 🟠 | ✅ | Segmented Controls: создан универсальный компонент `PillToggle` (PRODUCT.md §3.6). Заменены все хардкодные реализации в `workouts.tsx`, `settings.tsx` и `WorkoutDisplayModePicker`. Явный active state: `colors.primary` background, `textInverse`, `fontWeight: 600`, shadow, tap target ≥ 44pt. |
| LINT-1|🟠|✅|ESLint (eslint-config-expo + TS v8): **baseline 0 errors / 0 warnings** по всему репо (2026-09-23). Прежний baseline ~74 warning очищен в ходе аудита: unused imports/vars (profile.tsx, settings.tsx, programs.tsx, progress.tsx, exercise/[id].tsx), exhaustive-deps (workouts.tsx, progress/[id].tsx, useProfile, useWorkoutSession), hardcoded colors вынесены в constants. Чистота поддерживается как merge gate вместе с `tsc --noEmit`|
| SEC-11 | 🟠 | ✅ | 7 `SECURITY DEFINER` RPC в `public` были доступны роли `anon` (прямой грант + грант `PUBLIC`) — вызовы шли через `/rest/v1/rpc/…` без входа. Закрыто миграцией `20260924051013_revoke_anon_rpc_grants`: `revoke execute … from anon, public`, грант `authenticated` сохранён. Проверено end-to-end: анонимный вызов отвечает 401 `42501`, триггеры `on_auth_user_created` и event trigger `ensure_rls` целы, lint `anon_security_definer_function_executable` исчез |
| SEC-12 | 🔴 | ✅ | `copy_program_for_user(p_program_id, p_user_id)` не проверял `auth.uid()`, хотя он `SECURITY DEFINER` (обходит RLS как `postgres`) — любой входивший пользователь мог скопировать программу на чужой id. Закрыто миграцией `20260924051742_copy_program_for_user_uid_check`: guard `IF p_user_id IS DISTINCT FROM auth.uid() THEN RAISE` (именно `IS DISTINCT FROM`, иначе `NULL auth.uid()` проходил бы). Проверено: md5 тела без guard совпал с телом до миграции (`7577aecdb91124b96a8a96f7a2b02f18`) — изменено только добавленное условие; happy-path под JWT-клеймами `authenticated` возвращает новый id, чужой `p_user_id` даёт `P0001`, тестовые строки откатаны; `SECURITY DEFINER` и `search_path=public` сохранены. Все 3 места вызова (`useProgramEditor`, `usePrograms`→`copyProgramForUser`, `importProgramByCode`) передают `userId` из `useStore`, а тот ставится из `session.user.id` (`app/_layout.tsx:60,85`) — инвариант не ломается |
| SEC-13 | 🟢 | ⏸️ | `exercise_media` оставлена намеренно (решение 2026-09-24): RLS вкл, политик 0 → таблица закрыта на чтение всем ролям. Это нормализованная схема медиа из первой миграции (`4e2b509`), рассчитанная на несколько изображений/гифок на упражнение; UI всегда читал денормализованную `exercises.media_url` (`app/exercise/[id].tsx:220`, `ExerciseCard.tsx:107`, `AlternativeExerciseCard.tsx:251`). Данные не уникальны: все 821 строка совпадают по `url` с `exercises.media_url`, `source='legacy'`. Продуктового плана на мульти-медиа в `PRODUCT.md`/`ROADMAP.md` нет, поэтому ни политика, ни drop не нужны. Побочный эффект: lint `rls_enabled_no_policy` (INFO) будет показывать эту таблицу постоянно — это ожидаемо. Если медиа-фича понадобится, таблица готова (FK cascade, `UNIQUE (exercise_id, position)`, `CHECK media_type`), backfill тривиален |
| SEC-14 | 🟡 | ✅ | 14 функций `public` без `search_path` зашиты миграцией `20260924053444_pin_function_search_path` (`search_path = public, pg_temp`). Выбран `public, pg_temp`, а не `pg_catalog, pg_temp`: `migrate_exercise_equipment` обращается к `exercises` без схемы, а `normalize_equipment_array` вызывает `normalize_equipment_item` тоже без схемы. `pg_temp` указан явно и последним, потому что PostgreSQL неявно ставит `pg_temp` в НАЧАЛО пути, если он не перечислен. Все 14 — `SECURITY INVOKER`, то есть это устойчивость, а не эскалация (в отличие от SEC-11/SEC-12). Проверено: lint `function_search_path_mutable` исчез (14 → 0), `normalize_equipment_array(['D-Образная Рукоять','Гантель'])` по-прежнему резолвит вложенный вызов, все 5 триггерных функций (`set_updated_at`, `update_updated_at_column`, `handle_workouts_updated_at`, `fill_search_text`, `set_exercise_search_text`) отработали на временной таблице — `search_text` собран, `updated_at` проставлен; в прод-таблицы не писали. Функции `pg_trgm` (`similarity`, `gtrgm_*`, `set_limit`) не тронуты — они принадлежат расширению |
| SEC-15 | 🟡 | ✅ | Drift истории миграций устранён (2026-09-24). Причина оказалась глубже «части файлов нет в истории»: `version` в `supabase_migrations.schema_migrations` — первичный ключ, а 17 из 18 локальных файлов использовали 8-значные префиксы-даты, не уникальные (`20260828` ×4, `20260924` ×4, `20260820`/`20260904`/`20260913` ×2), поэтому `migration repair` физически не мог их выразить; имевшиеся 6 записей были созданы MCP-сервером с авто-таймстампами (`20260924051013` и т.п.), которые CLI из имён файлов не вычисляет, то есть `db push` считал неприменёнными все 18. Сделано: файлы переименованы в уникальные 14-значные версии, где версия = время коммита, добавившего файл, в UTC (`git log --diff-filter=A`); для уже отслеженных записей сохранена уже записанная версия, чтобы не переписывать факт применения; порядок внутри одной даты взят из git-хронологии, а не из алфавита (алфавит путался: `20260820_recommendation_feedback` добавлен раньше `20260820_drop_unused_session_tables`, а `workout_logs_is_warmup` вообще из 27 августа). Таблица истории приведена к тем же 18 версиям. Проверено механически: `diff` списка локальных файлов и `version || '_' || name || '.sql'` из таблицы — расхождений 0, по 18 с обеих сторон. Попутно исправлены ссылки на миграции в `INVENTORY.md` (в том числе несуществовавшее имя `20260828_workout_logs_is_estimated_reps.sql` → реальный файл) и в `STATUS.md`. Ограничение: у 12 вставленных записей `statements`/`rollback` пустые — для `db push` значим только `version`, но `supabase migration fetch` их SQL не вернёт |
| SEC-16 | 🟡 | ✅ | `pg_trgm` перенесён из `public` в служебную схему `extensions` миграцией `20260924054729_move_pg_trgm_to_extensions` (`alter extension pg_trgm set schema extensions`). Проверено сравнением «до/после»: оба GIN-индекса (`exercises_search_trgm_idx`, `idx_exercises_name_norm_trgm`) остались `indisvalid + indisready` (в `pg_index` хранятся OID оператора/opclass, а не имена со схемой, поэтому пересоздание не нужно); план `search_text ilike '%жим лёжа%'` идентичен базовому — тот же `Bitmap Index Scan on exercises_search_trgm_idx`; PostgREST-поиск отвечает 200. Побочный эффект в плюс: `rpc/similarity` больше не доступен через API (400). Что помнить: новые `CREATE INDEX ... gin_trgm_ops` и явные вызовы `similarity()` теперь требуют квалификации `extensions.`. Утверждение «приложению это не нужно» было ошибочным — его разбор и исправление в SEC-19. Мелкий хвост: `show_trgm` всё ещё описан в `src/types/database.types.ts:1244` (тип сгенерирован, когда функция была в `public`) — в коде не используется, исчезнет при следующей регенерации типов |
| SEC-17 | 🟡 | 🔲 | Отключён leaked-password protection в Auth (lint `auth_leaked_password_protection`) — проверка паролей по базе HaveIBeenPwned. Это конфигурация Auth, а не SQL: включается тумблером Dashboard → Authentication → Password security (либо `PATCH /v1/projects/{ref}/config/auth`). Менять конфигурацию прод-проекта можно только по явному запросу |
| SEC-18 | 🔴 | ⏸️ | **Схема из пустой БД не воспроизводится** — проверено 2026-09-24 прогоном на изолированном локальном стеке в докере (клон проекта во временной папке, к проду подключений нет). Падение со второй применённой миграции: `ERROR: relation "exercises" does not exist (SQLSTATE 42P01)` в `20260819075346_backfill_movement_pattern.sql`. Причина не в порядке версий (его починил SEC-15), а в отсутствии базовой схемы: миграции создают только 3 таблицы из 24 (`recommendation_feedback`, `cycle_events`, `cycle_settings`), остальные 21 (`exercises`, `workouts`, `profiles`, `program_*`, `daily_readiness` и др.) не создаются ни одним файлом — исходная схема получена мимо миграций. Дополнительно `20260731122543_sync_program_changes.sql` и `20260801050805_new-migration.sql` — нулевого размера (пустышки `supabase migration new`). **Решение: отложено 24.09.2026**, на работу приложения не влияет (прод и так в целевом состоянии, история с репозиторием синхронизирована). Для возобновления: squash к baseline — (1) очистить 18 записей истории, (2) `supabase db pull` для снапшота текущей схемы, (3) архивировать старые 18 файлов, (4) доказать результат прогоном `db reset` + пустым `db diff` против прода. Известные препятствия: `db pull`/`migration repair` через CLI валятся на соединении (diff даёт `read ECONNRESET` на пулере, прямое подключение `db.<ref>.supabase.co` требует IPv6, которого в сети нет; `--skip-pooler` поэтому не работает) — обходить через `pg_dump` из контейнера `supabase/postgres` или повтор с сессионным пулером 5432. Ход работ: шаг 1 уже выполнялся (18 записей удалены), затем полностью откачен до начала шага 2 — история сверена и восстановлена в исходном виде (18 строк, те же версии), временные проекты с паролем удалены |

| SEC-19 | 🔴 | ✅ | **Регресс SEC-16: справочник упражнений лежал с 24.09** («Не удалось загрузить упражнения», `app/(tabs)/exercises.tsx:114`; тот же текст в `src/components/program/sheets/ExercisePickerSheet.tsx:520`). Первопричина подтверждена вызовом на проде: `ERROR 42883: function word_similarity(text, text) does not exist / CONTEXT: SQL function "search_exercises" during startup`. `search_exercises` объявлена с `search_path = public` и вызывала trgm-функции без схемы (2× `word_similarity`, 1× `similarity`); после переноса `pg_trgm` в `extensions` имена перестали резолвиться. Падение на построении плана, то есть при любом вызове — и с `q IS NULL` тоже, поэтому список пустой даже без поиска. Данные, RLS и гранты ни при чём: 826 упражнений в `exercises`, `authenticated`/`anon` имеют `EXECUTE`, сигнатура совпадает с параметрами клиента. Исправлено (вариант B — квалификация вызовов, а не расширение `search_path`) миграцией `20260924085939_fix_search_exercises_trgm_schema`, применена через MCP. Проверено: проба `begin … rollback` до применения; `qualified_calls=3`, «голых» вызовов `0`; `proconfig` (`search_path=public`), `provolatile='s'`, `prosecdef=false` и ACL (`=X/postgres, postgres, anon, authenticated, service_role`) сохранены `CREATE OR REPLACE`; поведение — страница 1 и 2 по 40, `жим` и опечатка `присиданя` (fuzzy по trgm) дают результаты, сортировка `popularity` работает; тело в проде и в файле совпадает побайтово (md5 нормализованного `c88e1f93ad4b4829254ac26f9d624f92`). Масштаб: из всех функций `public` trgm-имена встречались только здесь; GIN-индексы перенос пережили (см. SEC-16) |

## 14. Feature 2: Muscle Map Visualization (Stage H)

| ID | Пр. | Статус | Цель |
|---|---:|---|---|
| H-MUSCLE-1 | 🟠 | ✅ | Анатомическая карта мышц в Workout Report: полноразмерная карта (front/back рядом) + легенда с сетами/тоннажем. Цвет шкалы интенсивности берётся из `colors.primary` текущей темы (автоперекраска при смене темы). Расчёт нагрузки через `calculateMuscleLoad`: эффективные сеты primary=100% / secondary=50%, нормированные относительно максимума; RPE/RIR не участвуют в цвете и остаются отдельным сигналом субъективной интенсивности. Компонент: `MuscleLoadMap.tsx` (переиспользуется в Progress hub). Удалены: `MuscleMapCompact.tsx`, `MuscleMapSheet.tsx`, локальная копия `libs/react-native-body-highlighter` (перемещены в `.trash/`). |
| H-MUSCLE-2 | 🟠 | ✅ | Progress hub: новая секция «Мышцы» (CI-4 Muscle Volume Analysis расширен до отдельной UI-зоны) с 3 вкладками: **Нагрузка** (периоды 7/30/90/всё + карта + легенда), **Усталость** (карта по бакетам 0–2/3–5/6–14/15+ дней + список мышц с днями), **Сила** (карта по сохранению e1RM + топ-5 упражнений). Источники: `muscleStatsService` (единственный Supabase-запрос) + `useMuscleStats` (React Query, staleTime 5 мин). **Исправлено (2026-09-17)**: реализованы отдельные SVG-пути для женской анатомии (экспорты `bodyFemaleFront` / `bodyFemaleBack` в `src/constants/muscleSvgPaths.ts`) вместо заглушки, которая использовала мужские пути. Теперь `BodyMap.tsx` корректно отображает женские пропорции тела (более узкие плечи, более широкие бёдра). |
| H-MUSCLE-3 | 🟠 | ✅ | **Тап-фильтр в Workout Report**: тап по окрашенной мышце на карте или в легенде фильтрует список упражнений, оставляя только те, что нагружали эту мышцу. Повторный тап снимает выделение и фильтр. Реализовано через `selectedSlug` в `BodyMap` + `exerciseHasMuscle` в `muscleLoad.ts`. |
| H-MUSCLE-4 | 🟠 | ✅ | **Карта покрытия в Program Detail**: `ProgramMuscleMap` показывает средневзвешенные плановые сеты/неделю по всем фазам программы. Помогает ответить на вопрос «подходит ли мне программа?» через визуализацию баланса мышечных групп. |
| H-MUSCLE-5 | 🟠 | ✅ | **Карта зон в Injuries**: `InjuryBodyMap` над chips-фильтром. Визуально подсвечивает зоны активных травм цветом по severity. Тап по мышце синхронно переключает фильтр зоны (arms/torso/legs), обеспечивая консистентность между картой и chips. |
| H-MUSCLE-6 | 🟠 | ✅ | **Переключатель режима нагрузки + модель OpenGym**: добавлен `MuscleLoadModeToggle` (Общий объём / Прямая нагрузка). **Критическое исправление формулы**: интенсивность подсветки на карте теперь считается ТОЛЬКО по эффективным подходам (`loadScore = sets`), а не по тоннажу (`weight × reps`). Это устраняет искусственное раздувание нагрузки на мышцы-помощники (трицепс, дельты, трапеция), которые получали огромный объём из-за большого веса в базовых упражнениях, несмотря на роль secondary. В режиме `direct` вторичные мышцы полностью исключаются. UI мгновенно пересчитывается без новых запросов. |

## 15. Update rule

После изменения кода:
1. закрыть/изменить соответствующий ID;
2. добавить новый ID, если работа появилась впервые;
3. не копировать сюда архитектурные правила — они принадлежат `CLAUDE.md`;
4. не копировать сюда карту файлов — она принадлежит `INVENTORY.md`;
5. дату среза обновлять при существенном изменении статуса;
6. Performance metrics обновляются после каждого замера (секция 12).
